{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build",
      "type": "shell",
      "command": "mkdir -p build && bear --output build/compile_commands.json -- pebble build",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
